import { View } from "react-native";

// Este componente nunca será visto pelo usuário.
// Ele serve apenas como um placeholder para a rota.
export default function CreatePostScreen() {
  return <View />;
}
